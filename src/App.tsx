import React, { FC } from 'react';
// import { Button } from 'antd';
import './App.scss';

import Seed from '../src/components/seed/seed'

const App: FC = () => (
  <div>
    <Seed />
  </div>
);

export default App;